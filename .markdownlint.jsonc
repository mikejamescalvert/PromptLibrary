{
  // Allow long lines in tables and code blocks
  "MD013": false,
  // Allow duplicate headings across different files (e.g., "## Purpose" in every prompt)
  "MD024": { "siblings_only": true },
  // Allow inline HTML if needed
  "MD033": false,
  // Allow fenced code blocks without a language (prompt blocks are plain text)
  "MD040": false
}
